# Runtime (API + agents). De Streamlit-stack staat in requirements-legacy.txt
# en is alleen nodig voor legacy_streamlit/ — niet installeren in de container,
# anders OOM't de Railway-build (exit 137).
requests==2.32.3
python-dotenv==1.0.1
google-genai>=1.0.0
pandas>=2.0.0
pydantic>=2.0.0
ortools>=9.10
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
apscheduler>=3.10.0
httpx>=0.27.0
